---
title: Quick Start
description: Create your first kipferl app in 5 minutes
---

# Quick Start

Let's build a simple CLI app that demonstrates kipferl's key features.

## Create a Project

```bash
mkdir hello-cli && cd hello-cli
kipferl init
```

## Write Your App

Create `app.py`:

```python
import tui
import input

# Show a welcome box
tui.box("Welcome to Hello CLI!", title="Hello", border_color="cyan")

# Interactive selection
action = input.select("What would you like to do?", [
    "Say hello",
    "Show a table", 
    "Exit"
])

if action == "Say hello":
    name = input("What's your name? ")
    tui.success(f"Hello, {name}!")
    
elif action == "Show a table":
    tui.table([
        ["Language", "Status"],
        ["Python", "Supported"],
        ["Rust", "Native"],
    ], headers=True, border="rounded")
    
elif action == "Exit":
    if input.confirm("Are you sure?"):
        tui.error("Goodbye!")
```

## Run It

```bash
kipferl run app.py
```

## Build a Binary

```bash
# Build for current platform
kipferl build app.py -o hello

# Build a standalone binary for this machine
kipferl build app.py -o hello --mode universal

# Or target another supported platform
kipferl build app.py -o hello-linux --target linux-x86_64

# Run the binary
./hello
```

## What's Next?

<Cards>
  <Card title="tui Module" href="/docs/modules/tui" />
  <Card title="input Module" href="/docs/modules/input" />
  <Card title="All Modules" href="/docs/modules" />
</Cards>
