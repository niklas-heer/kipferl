# Test fixtures may panic on failed setup/assertions; production lint policy
# remains enforced across every workspace crate.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
allow-indexing-slicing-in-tests = true
